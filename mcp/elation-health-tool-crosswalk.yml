generated: '2026-07-24'
method: derived
source: mcp/elation-health-mcp.yml, openapi/*.json
note: >-
  Elation's published MCP server (docs.elationhealth.com/mcp) is a ReadMe
  generic OpenAPI-navigation server: its five tools are meta-tools over the spec,
  not one tool per Elation resource/operation. There is therefore no 1:1 binding
  between an MCP tool and a single OpenAPI operationId. The three read tools
  navigate the whole spec; execute-request can invoke ANY operation via a HAR
  request object. All REST operations remain reachable through execute-request,
  but none has a dedicated named tool. GraphQL is not offered.
surfaces:
  openapi:
    files: 17
    operations: 846
    gated: false
  graphql:
    present: false
  mcp:
    url: https://docs.elationhealth.com/mcp
    tools_list_gated: false
    style: generic-openapi-navigation
crosswalk: []
mcp_only:
  - tool: list-endpoints
    reason: Meta-tool; enumerates all spec paths/methods. No single backing operationId.
  - tool: get-endpoint
    reason: Meta-tool; returns spec detail for a chosen path+method. No single backing operationId.
  - tool: search-endpoints
    reason: Meta-tool; keyword search across the spec. No single backing operationId.
  - tool: list-specs
    reason: Meta-tool; lists available OpenAPI specs. No backing operationId.
  - tool: execute-request
    reason: >-
      Generic executor. Can invoke ANY of the 846 REST operations via a HAR
      request object, so it fans out to the entire spec rather than binding to
      one operationId. Caller supplies method+url+auth.
rest_only:
  note: >-
    All 846 REST operations are executable only through the generic
    execute-request tool; none is exposed as a dedicated named MCP tool.
  capabilities:
    - patient-profile (allergies, problems, medications, immunizations, vitals, histories)
    - scheduling (appointments, appointment types)
    - orders (lab, imaging, cardiac, pulmonary, sleep, referral)
    - patient-document
    - billing
    - insurance and eligibility
    - practice and user management
    - messaging
    - event subscriptions / webhooks
    - reference data
    - care gaps
    - data import
coverage:
  tools_named: 5
  tools_bound_to_single_operation: 0
  mcp_only: 5
  rest_operations_total: 846
  rest_operations_with_dedicated_tool: 0
  rest_operations_reachable_via_execute_request: 846
